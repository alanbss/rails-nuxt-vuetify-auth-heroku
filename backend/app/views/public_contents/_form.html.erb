<%= form_with(model: public_content) do |form| %>
  <% if public_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(public_content.errors.count, "error") %> prohibited this public_content from being saved:</h2>

      <ul>
        <% public_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
