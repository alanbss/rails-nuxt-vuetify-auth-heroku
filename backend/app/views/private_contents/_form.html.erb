<%= form_with(model: private_content) do |form| %>
  <% if private_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(private_content.errors.count, "error") %> prohibited this private_content from being saved:</h2>

      <ul>
        <% private_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
